version: "2.1"

services:
    nginx:
        networks:
            nw_internal:
            nw_external:
                 ipv4_address: ${NETWORK_IP}
#                link_local_ips:
#                  - ${NETWORK_IP}

networks:
    nw_external:
        external:
            name: ${NETWORK_NAME}
    nw_internal:
